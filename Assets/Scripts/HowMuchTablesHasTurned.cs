using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HowMuchTablesHasTurned : MonoBehaviour
{
    public float turnedTableCount;
    public float triggerCount = 2;
    // Start is called before the first frame update
    void Start()
    {
        turnedTableCount = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
